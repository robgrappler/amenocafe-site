{
  "name": "ameno-cafe-site",
  "private": true,
  "description": "Node module config and test script for the Ameno Cafe static site",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && cp -R assets client-intake copy-editor src favicon.ico dist/",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "^6.1.5"
  }
}
